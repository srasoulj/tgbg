#############################################
# Go web app – sample environment file
#############################################

# These settings must point at the SAME MySQL database
# that the desktop sync app writes to.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=telegram_reader
DB_USER=tgbg
DB_PASSWORD=your_db_password_here

